# 작업 규칙

## 파일 설명

- 작업한 내용 중 추후에 재사용하거나 참고해야할 부분은 code.md에 간결하게 기록한다.
    - 작성 형식: 작성한 파일별 사용법, 기능, 특이사항(나중에 고려할 사항)
- 데이터셋 등 대규모 파일은 ~/jairlab/data/ 폴더에 저장할 것. 작업 공간에 필요시 symbolic link 사용한다.
- 쓸데없는 파일을 남기지 않는다. 임시 파일은 사용 후 삭제한다.
- 파일에 수정 사항이 생기면 일부만 수정하는 게 아니라, 처음부터 깔끔하게 다시 작성한다. 편집 흔적을 남기지 않는다.

## 기본 규칙

- **모든 작업의 최종 목표는 연구의 성공이다. 사용자의 요구가 연구에 문제를 발생시키는지 객관적으로 판단한다.**
- 메모리 기능 쓰지 않는다. 세션을 동시에 여러 개 사용 시, 충돌이 발생한다.
- 답변은 한국어로 작성한다. 번역투를 쓰지 않는다.
- '~' 대신 '-'를 사용한다. (md 문법 때문에 취소선으로 보임)
- 모든 작업에 Todos를 작성한다.
- 워크스페이스는 Github 표준 구조를 사용한다.
- 모든 단계의 파일럿 테스트에는 시각화 체크(렌더링 결과)를 포함한다.
- 파일럿 테스트와 본 실행은 하나의 파일에서 실행한다. (파라미터로 파일럿 테스트 / 본 실행 결정)
- 오랜 시간이 걸리는 작업은 모니터링을 진행한다. (모니터링은 시간 기준으로 진행, 이벤트 트리거 X)
- 사용자 요청 없이 git 관리하려고 하지마.

### 코드 작성 유의 사항

- 호출 사이에 유지되는 상태가 있으면 클래스, 없으면 모듈 수준 함수로 작성한다. (중첩 함수는 금지)
- 각 클래스는 하나의 책임만 가진다. (SOLID의 단일 책임 원칙 준수)
- 속성은 private/protected 관례(_ 또는 __)를 사용해 캡슐화하고, 필요하면 getter/setter(@property)를 쓴다.
- 가급적 인터페이스나 추상 클래스(abc 모듈)를 활용해 결합도를 낮춘다.
- 코드 실행 과정에서 모니터링할 수 있게끔 로그를 출력하도록 작성한다. (최종 출력 로그 X, 실행 중 출력 로그 O)
- CPU 사용 최소화, GPU 사용을 최대화하도록 코드를 작성한다.

### 주석 작성 규칙

- 모든 클래스·함수(내부용 _함수 포함)에 docstring을 작성한다.
    - 역할 한 줄 + 입출력 의미. 기하 계산이면 좌표계·단위·부호 규약까지 명시한다.
- 함수 본문은 빈 줄로 구분되는 논리 블록마다 주석 1줄을 단다.
    - "무엇"의 반복이 아니라 "왜 / 무엇을 보장하는지"를 쓴다.
    - 단순 대입·return 등 자명한 블록은 예외.
- 수식·기하 계산에는 유도 근거를 주석으로 남긴다. (어떤 정리·규약에서 나온 식인지, 변수의 물리적 의미)
- 주석은 코드 위에 작성한다. (코드 옆에 작성 X)
- 코드를 말로 옮기기만 한 주석은 쓰지 않는다.
- 주석을 히스토리처럼 사용하지 않는다.
    - 예시: 이전 작업에서 파라미터 값이 잘못되어서 변경하였다.
    - 히스토리는 code.md에 작성한다.

### 코드 실행 유의 사항

- **호스트(로컬) 환경을 건드리지 않는다.** 컨테이너에서만 동작
- 코드 실행 시, 병렬 처리 및 멀티 프로세스를 사용하여 시간을 단축한다.
- python, shell 스크립트 실행 등 프로세스 실행 전에 어떤 작업인지 설명한다.

## Docker 관련 규칙

- 컨테이너 이름은 `airlab_hw_{컨테이너 이름}`, 이미지 이름은 `t0mark251/{이미지 이름}`으로 작성한다.
- GPU는 0~3번 GPU만 사용한다.
- 도커 실행 및 빌드 관리는 compose를 사용한다.
- 초기 도커 파일 검증 방법
    - 초기 도커파일 빌드 후, 의존성 문제는 반드시 발생함
    - 초기 도커 파일만 빌드하고, 의존성 문제는 최대한 컨테이너 내부에서 해결
    - 컨테이너 내부에서 해결 완료 및 코드 테스트 성공 후에 도커 파일에 반영 -> 빌드는 최소 횟수로만 수행
- 컨테이너 마운트
    - ~/jairlab/hw/{작업 폴더}/:/workspace/
    - ~/jairlab/data/:/data/
